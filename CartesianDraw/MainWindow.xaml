<Window x:Class="CartesianDraw.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:CartesianDraw.View"
        mc:Ignorable="d"
        Title="Cartesian Draw"
        SizeToContent="WidthAndHeight">
    <Border Style="{StaticResource BorderSurroundWindowStyle}">
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <local:AddRemoveEditPrimitiveView Grid.Column="0"/>
            <local:CartesianCanvasView Grid.Column="1"/>
        </Grid>
    </Border>
</Window>
